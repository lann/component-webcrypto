[package]
name = "conformance-guest"
version = "0.1.0"
edition = "2021"
publish = false
description = "The shared conformance guest: executes the Wycheproof-derived vector cases and the API-contract probes against whichever lann:webcrypto implementation the target provides."

[lib]
crate-type = ["cdylib"]

[dependencies]
# The lann:webcrypto bindings and stream helpers; carries the imports, so
# this component's own world declares only its export.
lann-webcrypto-guest = { path = "../../guest-sdk" }
wit-bindgen = { version = "0.59", features = ["async"] }
futures = { version = "0.3", default-features = false, features = ["async-await"] }
# Parsing the vendored Wycheproof JSON compiled into the guest.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
